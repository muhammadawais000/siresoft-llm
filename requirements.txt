# --- Core web framework ---
Django>=5.0,<5.1
djangorestframework>=3.15
django-environ>=0.11
django-cors-headers>=4.4
drf-spectacular>=0.27

# --- Database ---
psycopg[binary]>=3.1
pgvector>=0.3          # historical migration 0001 still imports pgvector.django -- Chunk itself no longer uses it

# --- Vector store (dedicated, containerized) ---
# Pinned to stay within one minor version of the qdrant/qdrant server
# image tag in docker-compose.yml -- qdrant-client warns/can break when
# the gap is wider, so bump both together, not just whichever resolves
# newest on a fresh `pip install`.
qdrant-client>=1.11,<1.13

# --- Background jobs ---
celery>=5.4
redis>=5.0
django-celery-results>=2.5

# --- LangChain / RAG ---
langchain>=0.2
langchain-community>=0.2
langchain-huggingface>=0.0.3
langchain-ollama>=0.1
langchain-text-splitters>=0.2

# --- Ingestion loaders ---
pymupdf>=1.24          # PyMuPDFLoader backend
docx2txt>=0.8           # Docx2txtLoader backend
pytesseract>=0.3.10      # OCR engine binding (requires system tesseract-ocr binary)
pdf2image>=1.17          # renders PDF pages to images for OCR fallback (requires system poppler-utils)
trafilatura>=1.9         # URL loader / boilerplate stripping
chardet>=5.2             # encoding auto-detection for TXT/MD

# --- Embeddings / re-ranking ---
sentence-transformers>=3.0
torch>=2.2

# --- Utilities ---
tiktoken>=0.7
gunicorn>=22.0
requests>=2.31
