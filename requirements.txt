# --- Core web framework ---
Django>=5.0,<5.1
djangorestframework>=3.15
django-environ>=0.11
django-cors-headers>=4.4
drf-spectacular>=0.27

# --- Database / vector store ---
psycopg[binary]>=3.1
pgvector>=0.3

# --- Background jobs ---
celery>=5.4
redis>=5.0
django-celery-results>=2.5

# --- LangChain / RAG ---
langchain>=0.2
langchain-community>=0.2
langchain-huggingface>=0.0.3
langchain-ollama>=0.1
langchain-text-splitters>=0.2

# --- Ingestion loaders ---
pymupdf>=1.24          # PyMuPDFLoader backend
docx2txt>=0.8           # Docx2txtLoader backend
pytesseract>=0.3.10      # OCR engine binding (requires system tesseract-ocr binary)
pdf2image>=1.17          # renders PDF pages to images for OCR fallback (requires system poppler-utils)
trafilatura>=1.9         # URL loader / boilerplate stripping
chardet>=5.2             # encoding auto-detection for TXT/MD

# --- Embeddings / re-ranking ---
sentence-transformers>=3.0
torch>=2.2

# --- Utilities ---
tiktoken>=0.7
gunicorn>=22.0
requests>=2.31
